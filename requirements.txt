flask==3.1.3
flask-limiter==4.1.1
waitress==3.0.2
supabase==2.31.0
groq==1.0.0
python-dotenv==1.2.2
requests==2.32.5
# polars-lts-cpu (no "polars" a secas): mismo paquete/API, pero un solo
# binario sin runtime-selection (evita el split polars+polars-runtime-32,
# que en Vercel pesaba ~176MB extra). Sin AVX2 — irrelevante para el
# volumen de datos de este proyecto (miles de filas de CSV, no big data).
polars-lts-cpu==1.33.1
numpy==2.4.2
joblib==1.5.3
# xgboost-cpu (no "xgboost" a secas): xgboost==3.2.0 declara
# nvidia-nccl-cu12 como dependencia INCONDICIONAL en Linux (confirmado en
# PyPI), aunque no usamos GPU en absoluto — hubiera sido un desastre de
# tamaño. xgboost-cpu es el mismo paquete (mismo import xgboost, misma
# API) sin esa dependencia. scipy sigue viniendo igual (dependencia dura
# tanto de xgboost como de xgboost-cpu, no hay forma de evitarlo con la
# API pública de pip/PyPI — se probó con --no-deps vía buildCommand
# personalizado y NO funciona: Vercel resuelve dependencias de Python por
# su cuenta a partir de este archivo, ignora buildCommand para eso).
xgboost-cpu==3.2.0
