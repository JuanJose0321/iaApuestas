-r requirements.txt

# xgboost no está en requirements.txt a propósito — en Vercel se instala aparte
# con --no-deps (ver vercel.json) para no arrastrar scipy (135MB) al bundle.
# Para dev/CI no importa el tamaño, así que aquí se instala normal.
xgboost==3.2.0

pytest==9.0.3
mypy==2.3.0
vulture==2.16
pre-commit==4.6.1
